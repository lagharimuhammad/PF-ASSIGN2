24K-0006

we make a function that compresses each word by removing duplicate characters keeping only the first occurrence of each character.
it iterates through each word,it checks if it has already been added to a compressed_word array for that word.
finally, it prints each compressed word.